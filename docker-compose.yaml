version: "3.8"
services:
#  reflex_app:
#    image: django-polls-app
#    ports:
#      - 8000:8000
#    networks:
#      - sturdy_palm_tree
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: ${PG_USER}
      POSTGRES_PASSWORD: ${PG_PASSWORD}
      POSTGRES_DB: ${PG_DATABASE}
    ports:
      - "5432:5432"
    networks:
      - sturdy_palm_tree
networks:
  sturdy_palm_tree:
    driver: bridge